import { CaracteristicasVehiculo } from './caracteristicas-vehiculo.model';

describe('CaracteristicasVehiculo', () => {
  it('should create an instance', () => {
    expect(new CaracteristicasVehiculo()).toBeTruthy();
  });
});
