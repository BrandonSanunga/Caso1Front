import { IformeReclamoModel } from './iforme-reclamo-model';

describe('IformeReclamoModel', () => {
  it('should create an instance', () => {
    expect(new IformeReclamoModel()).toBeTruthy();
  });
});
